load("@rules_python//python:defs.bzl", "py_binary")

package(default_visibility = ["//visibility:public"])

py_binary(
    name = "detect_divergence",
    srcs = ["detect_divergence.py"],
    main = "detect_divergence.py",
)
