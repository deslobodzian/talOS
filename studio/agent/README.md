# Local headless JSON-RPC

Run `bazel run //studio:agent` from the repository root alongside the bridge. `TELEMETRY_URL` defaults to `ws://127.0.0.1:5800/telemetry`; `AGENT_PORT` defaults to 5802. This is a JSON-RPC 2.0 service, not an MCP implementation. It consumes the same binary decoder, jitter buffer, and bounded timeline as the UI, independently of any browser window. The query surface itself lives in `agent/rpc.ts`, which has no transport and no Node dependencies, so the Studio worker answers the Agent tab from the identical code path rather than a second implementation that would drift from this one; `agent/server.ts` re-exports it so existing import paths still work. WebSocket reconnects every second. Producer restarts require restarting the agent until a stream epoch is added to the protocol. The upstream connection carries two message families: binary telemetry frames and text system graphs. A frame fits in one datagram, but a graph describing every node on every topic is far larger, so the upstream payload cap is raised to 1 MiB and `parseSystemGraph` enforces the real bound — a cap below the message would drop the connection rather than the message. The 64 KiB limit above applies to requests coming in, which is unchanged.

POST JSON-RPC requests to `http://127.0.0.1:5802/rpc`, or send them on `ws://127.0.0.1:5802/rpc`. Batches and notifications are supported. Browser Origin headers and non-local Host headers are rejected; the socket binds only to loopback. Request size is limited to 64 KiB. GET `/health` reports upstream connectivity, retained frame count, and jitter statistics, plus a `system` block: how many system graphs have arrived, how many were rejected as malformed, and the node and topic counts from the latest one. That is reported separately from telemetry health on purpose — a node can be publishing frames while the registry is unreadable, and the reverse. A `declared` block reports the same for the declared graph, with `received: false` when the bridge was started without `--declared`; its node counts are absent rather than zero in that case, because "nobody declared anything" and "no declared graph was offered" are different answers.

- `get_schema_tree`, params `{}`: returns observed leaf topics and JavaScript value types, retained timestamp bounds, frame count, and timestamp encoding. `channels` entries also have direct topic aliases. Schema discovery describes retained data, not fields never received.
- `query_state_at`, params `{"timestamp_ns":"123","topics":["chassis.x","motor.current"],"mode":"exact"}`: returns a complete snapshot when topics are omitted; otherwise a timestamped topic map. Nanoseconds are decimal strings to preserve uint64 precision. Exact is the default and returns error `-32001` if no retained sample has that timestamp. Explicit `at_or_before` selects the preceding sample. No interpolation is introduced into agent results.
- `scan_channel_events`, params `{"topic":"motor.current","condition":{"op":"gt","value":40},"start_ns":"0","end_ns":"999999999999"}`: returns contiguous matching **sample** ranges with inclusive endpoints and sample counts. Missing packets are not inferred to match. Operators: `gt`, `gte`, `lt`, `lte`, `eq`, `ne`, `changed`, `rising`, `falling`. Edge comparisons use the preceding retained sample even if it precedes start_ns. No arbitrary code or expression evaluation is accepted.

- `get_system_graph`, params `{}`: returns the live node registry as the bridge last sent it — every node with its pid, session, liveness and full source list, every topic with both ends named, and a `summary` counting nodes alive and topics that are orphaned, unread or lossy. Each source carries `events_per_second` derived from the previous graph, or `null` when only one graph has arrived: a rate needs two samples, and "not measured yet" is not the same answer as "not moving". Each topic carries a `health` of `ok`, `orphaned` (nothing publishes it), `unread` (nothing subscribes), `lossy` (messages were dropped), `idle` (wired up, nothing published yet), `bridged` or `unconnected`. The last two are half-wired topics that are not faults, and they are separated from the first two because reporting a design as a fault is what teaches people to ignore a fault report: `bridged` means the end that has no shared-memory peer declared itself `external`, so its peer is outside talOS — `/hw/command` is consumed by the RoboRIO over UDP and no subscriber will ever appear — and `unconnected` means the end declared itself `optional`, so it is allowed to be waiting, as `/drivetrain/target/auto` is until autonomous is written. Every source carries the `external` and `optional` booleans it was declared with, and `summary.topics_by_health` tallies every state so a caller can count faults without having to know which states are faults. The result also reports the graph's `version` and `summary.endpoint_attributes`: a bridge older than system-graph version 2 could not state where a far end lives, so against one of those documents `bridged` and `unconnected` are unreachable and the topics they describe are reported as faults, which is the answer that bridge always gave. This answers a question telemetry cannot: a frame shows what one node chose to publish, not the shape of the system that produced it.
- `describe_topic`, params `{"topic":"/odometry/state"}`: one topic, with every node on either end of it and that end's kind, counters and rate. Saves filtering the whole graph to ask who writes a topic and whether anyone is reading it. Unknown topic names are rejected as `-32602` rather than returning an empty result, because a typo and a topic nobody is on should not look the same.

- `get_declared_graph`, params `{}`: what the launcher declared before it spawned anything. It probes every node in the config with `--describe` — which builds the node on a simulated loop and prints the ends its constructor registered — assembles the results into one graph, lints it, and writes it out; the bridge serves that file at `GET /declared.json` when started with `--declared PATH`. Returns every declared node with its `target` and its ends, the launcher's lint `diagnostics` verbatim (`severity` of `error` or `warning`, the `subject` it is about, and the launcher's own sentence), and a `comparison`: how many nodes were declared, how many of them registered, which ones never did, and which running nodes were never declared. That comparison is the point. The live registry only ever holds what did start, so a name missing from it is either a node that crashed before it claimed a slot — or whose binary is missing — or a node that was never part of this session, and from the registry alone those are the same silence. `comparison` is `null` rather than a set of zeroes when no system graph has arrived, because "never registered" is not a claim that can be made without a registry to check against.

All three system methods fail with `-32002` when the document they read has not been received yet, which is deliberately distinct from a graph reporting no nodes. The first means the bridge has not told this agent anything; the second means the bridge looked and nothing is running. Returning an empty graph for the former would make a disconnected agent indistinguishable from a stopped robot. `get_declared_graph` fails the same way when the bridge was started without `--declared`, and says so: everything else keeps working, because a declared graph is an extra source of truth and not a prerequisite for reading the live one.

The sample `bazel run //studio/scripts:detect_divergence` obtains the latest exact snapshot, compares RawOdometry with FusedPose (falling back to chassis), and scans a producer divergence channel when present. Output is deterministic for identical retained frames. This is a bounded live window, not a persistent archive.

Desktop UDP uses Tauri v2 `Channel<Response>` binary IPC, avoiding JSON/base64 event payloads. The frontend must acknowledge each payload with `invoke('ack_udp')`; 32 unacknowledged packets is the hard in-flight limit. Overload drops fresh datagrams until capacity returns. A 50 ms socket timeout bounds shutdown. `stop_udp` closes the socket and joins the native listener; closing the application drops managed state and stops it. A new `start_udp` restarts the listener. The schema must fit in one UDP datagram; the bridge should enforce the stricter configured MTU payload budget.

References: [Tauri binary command responses](https://v2.tauri.app/develop/calling-rust/) and [Tauri channels](https://v2.tauri.app/develop/calling-frontend/).
