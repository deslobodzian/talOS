# Marks third_party as a package so http_archive build_file labels like
# //third_party:eigen.BUILD.bazel resolve. Third-party shims only; no
# first-party code here.
package(default_visibility = ["//visibility:public"])
