# Agent session worktrees: full repo checkouts nested under the workspace that
# must never be traversed by `bazel build //...` (their BUILD files resolve
# against the wrong output base and fail package loading).
.muse
